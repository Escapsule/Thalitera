spring:
  profiles:
    active: dev
  application:
    name: ${dev.application.name}
  datasource:
    url: ${dev.datasource.url}
    username: ${dev.datasource.username}
    password: ${dev.datasource.password}
    driver-class-name: ${dev.datasource.driver-class-name}
    hikari:
      connection-timeout: ${dev.datasource.hikari.connection-timeout}
      maximum-pool-size: ${dev.datasource.hikari.maximum-pool-size}

mybatis-plus:
  configuration:
    # 全局设置
    map-underscore-to-camel-case: ${mybatis-plus.configuration.map-underscore-to-camel-case}
    log-impl: ${mybatis-plus.configuration.log-impl}
    auto-mapping-behavior: ${mybatis-plus.configuration.auto-mapping-behavior}

    # PostgreSQL 特殊配置
    jdbc-type-for-null: ${mybatis-plus.configuration.jdbc-type-for-null}

  global-config:
    db-config:
      id-type: ${mybatis-plus.global-config.db-config.id-type}
      logic-delete-field: ${mybatis-plus.global-config.db-config.logic-delete-field}
      logic-not-delete-value: ${mybatis-plus.global-config.db-config.logic-not-delete-value}
      logic-delete-value: ${mybatis-plus.global-config.db-config.logic-delete-value}

  # Mapper 文件配置
  mapper-locations: ${mybatis-plus.mapper-locations}
  type-aliases-package: ${mybatis-plus.type-aliases-package}

# 日志配置（显示完整SQL）
logging:
  level:
    com.escapsule.thalitera.mapper: ${logging.level.com.escapsule.thalitera.mapper}